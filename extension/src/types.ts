export type Credentials = { api: string; token: string }
export type Recording = { tabId: number; demoId: string; api: string; token: string; screenshot: string; active: boolean }
export type Rect = { x: number; y: number; w: number; h: number }

