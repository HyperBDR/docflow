{
  "name": "docflow-extension",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": { "build": "node build.mjs", "watch": "node build.mjs --watch" },
  "devDependencies": { "@types/chrome": "latest", "esbuild": "latest", "typescript": "latest" }
  ,"dependencies": { "rrweb-snapshot": "2.1.0" }
}
